# CI — static fallback for `cron` projects.
# Prefer `./bin/wf harness ci`, which GENERATES this from harness.signals.
name: ci
on:
  push: { branches: [main] }
  pull_request:
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # set up the toolchain for your stack
      - name: install
        run: echo "TODO: install deps"
      - name: test
        run: echo "TODO: test command"
      - name: build
        run: echo "TODO: build command"
