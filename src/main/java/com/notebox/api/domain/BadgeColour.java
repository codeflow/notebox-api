package com.notebox.api.domain;

/** Fixed palette a List field's option badge may use (FR-03); no colours outside this set. */
public enum BadgeColour {
    RED,
    GREEN,
    BLUE,
    BLACK,
    GRAY,
    YELLOW
}
