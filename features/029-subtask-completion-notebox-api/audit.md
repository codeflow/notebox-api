# Audit — A subtask records when it was completed

**ID:** features/029-subtask-completion-notebox-api
**Date:** 2026-09-03
**Verdict:** pass with findings — **0 blockers**

> **Incomplete, and the gap is named.** Six axes were dispatched; **five returned**. The sixth —
> *scenario honesty*, which asks whether each test would fail if the behaviour regressed — died on
> an infrastructure error and was not re-run: the product owner chose to move on rather than
> re-dispatch it (2026-09-03). For a project whose whole discipline is refusing vacuous green,
> that is the one axis whose absence is worth stating at the top rather than in a footnote.

> An earlier dispatch of this same audit lost 11 of 12 agents to API 500s, and one of them died
> mid-probe leaving a deliberate mutation inside `Subtask.markDone` — the transition guard removed.
> It was found by `git status`, restored, and `mvn -B verify` re-confirmed green at 429 tests
> before this run.

## Axes that returned

- traceability
- Check 3
- compliance
- Check 5
- open-questions

## Findings (19)

### [major] workflow.json attributes feat-029 and feat-030 to FR-11 — the FR the spec proves is the wrong one

- **Axis:** traceability
- **Kind:** traceability
- **Evidence:** workflow.json:5498 and workflow.json:5701 both title the feature "Record when a subtask was completed, and complete it from the grid (FR-11, BR-06/BR-07) (US-4.1)". `./bin/wf status` renders exactly this string. FR-20 appears nowhere in workflow.json except inside one task title (workflow.json:5631, "T-06 — A pre-FR-20 row keeps reporting nothing"). Meanwhile spec.md:20-24 spends a full Origin paragraph arguing the opposite: "FR-11 enumerates a subtask's attributes as 'name, dates, optional card, done flag' and says nothing about **when** completion happened. Writing this spec against it would have stretched a requirement over surface it does not name." workflow.json was edited on this branch (384 lines in the diff), so the citation had every opportunity to be corrected and was not.
- **Failure scenario:** feat-030 (notebox-web) has not started; its spec step is the next eligible work after this audit. A fresh context opens the session, CLAUDE.md rule zero renders `./bin/wf status`, and the only FR the pipeline offers for feat-030 is FR-11. The web spec is then written with Origin: FR-11 — reproducing precisely the 'stretched requirement' failure CLAUDE.md calls the most expensive here and that FR-20 was created on 2026-09-02 to prevent. FR-20 has no feature pointing at it anywhere in the pipeline state, so on promotion nobody can answer 'which feature delivered FR-20?' from workflow.json.
- **Fix:** Retitle both feature steps to cite FR-20 (e.g. "… (FR-20, BR-06/BR-07 frozen) (US-4.1)") before feat-030.spec starts. Do it through `wf` rather than by hand-editing workflow.json, per CLAUDE.md §7.

### [major] catalogs/epics.md was never touched: US-4.1 does not list FR-20, still reads `delivered`, and has no History entry for the pair

- **Axis:** traceability
- **Kind:** traceability
- **Evidence:** `git diff develop...feature/subtask-completion -- catalogs/epics.md` is empty. catalogs/epics.md:51 still reads "| US-4.1 | … | FR-10, FR-11 · BR-06 | delivered | feat-010-… · feat-011-… |" — no FR-20, no feat-029, no feat-030. catalogs/epics.md:16 still reads "| E4 | Tasks & subtasks | … | delivered |". The History table ends at v22 (2026-08-26) with no entry for the 2026-09-02 decision or the feature pair. Yet catalogs/requirements.md:31 declares FR-20's owning story to be US-4.1, so the link is one-way: FR-20 → US-4.1 exists, US-4.1 → FR-20 does not. Precedent contradicts this: v15 and v22 each added a History row *at feature creation*, and v22 explicitly records "corrected here: E6 read `speccing` while US-6.1 has been delivered" — this project treats epics-status drift as a defect worth fixing.
- **Failure scenario:** instructions/feature-audit.md says the pass verdict's action is "update `catalogs/epics.md` to `delivered`". US-4.1 already reads `delivered`, so passing this audit writes nothing and the drift becomes permanent. Concretely: a Must-priority requirement (FR-20) is unshipped on both sides while its story and its epic both read `delivered`, and US-4.1's Feature column — the list /wf-promote reads to build the pair-wise promotion set — names only feat-010 and feat-011. The spec's own binding rollout hazard ("the pair is promoted together", spec.md Rollout hazard §2) is therefore recorded in no catalog a promoter reads, and feat-029 can be promoted to `main` alone: the API then erases a subtask's completion moment on any web PUT that omits `done`, with no screen reading the field.
- **Fix:** Amend catalogs/epics.md in this feature: add FR-20 (and BR-07) to US-4.1's FR/BR cell, move US-4.1 back to `building` (US-2.1/US-4.2/US-3.1 precedent: a story is delivered only when both sides ship), reopen E4 to `building`, append feat-029/feat-030 to the Feature column with the promote-together note, and add a History row for the 2026-09-02 decision mirroring requirements.md v4.

### [major] T-04 edits TaskServiceTest, which its file list omits — and tasks.md therefore declares T-05 parallel-safe when it is not

- **Axis:** Check 5
- **Kind:** plan-was-wrong / undeclared file
- **Evidence:** features/029-subtask-completion-notebox-api/tasks.md:57 lists T-04's files as SubtaskInput.java, messages.properties, messages_pt.properties, TaskMessageCoverageTest.java, TaskResourceTest.java — TaskServiceTest.java is absent. Commit 361885c (T-04) touches src/test/java/com/notebox/api/application/task/TaskServiceTest.java anyway (5 SubtaskInput constructions re-arity'd). tasks.md:71-76 declares T-05's file as TaskServiceTest.java with "parallel: **yes** — its own file, touched by no other task", and tasks.md:114 lists only T-03→T-04→T-07 as strictly sequential. The cause is that T-04 adds a component to the SubtaskInput *record*, whose canonical constructor is called from TaskServiceTest — the plan (plan.md:142) reasoned only about the `Subtask` 4-arg constructor and `setDone`, and missed `SubtaskInput`'s arity entirely.
- **Failure scenario:** Follow tasks.md as approved and run T-04 and T-05 in parallel worktrees, both branched from T-02. T-05's worktree sees the 5-component SubtaskInput and writes its four new tests as `new SubtaskInput("A", SEP_1, SEP_5, true, null)`; T-04's worktree changes the record to 6 components and rewrites the two shared helpers at TaskServiceTest:93/97 plus the call at :332. On merge, git conflicts on those three lines (both sides edited them), and once resolved the four T-05 tests still pass 5 arguments to a 6-component record: `mvn -B verify` fails to compile the test sources. The run that actually happened was sequential (361885c before 9a71329), so this never fired — but the artifact instructs the failing order.
- **Fix:** Add `src/test/java/com/notebox/api/application/task/TaskServiceTest.java` to T-04's file list, move T-05 to `parallel: no`, and extend tasks.md:114 to read T-03 → T-04 → {T-05, T-07}. State the real cause: any change to a record's component list is a repo-wide arity change, not a single-file edit.

### [major] The plan prices the clock seam at "two call sites", but one of those two is the constructor, and the contract forbids the only way to supply it a moment

- **Axis:** open-questions
- **Kind:** reversibility-claim-false / artifact-contradiction
- **Evidence:** plan.md:117 — reversibility row `| No clock seam | **reversible** | Two call sites, no schema change (see rejected alternative 4). |`. plan.md:104 — `**If it is ever needed the migration is two call sites** — markDone(boolean, Instant) with the caller supplying the moment`. contracts/subtask-completion.md:58 — `> constructor, and no constructor may take completedAt. Both would reopen the door this design closes.` Reality from the code: markDone has exactly two callers in src/main — src/main/java/com/notebox/api/domain/Subtask.java:66 (inside the 4-arg constructor, which the plan's own Approach section says delegates deliberately) and src/main/java/com/notebox/api/application/task/TaskService.java:155. The constructor is NOT a caller that can 'supply the moment': it holds no Instant. Its own callers are `new Subtask(...)` — src/main/java/com/notebox/api/application/task/TaskService.java:133 plus 38 test sites, 39 repo-wide today (30 on develop; this feature's own tests added 9). catalogs/open-questions.md:509 copies the bad price into the open OQ: `feat-029's plan rejects as out of proportion for one field but prices it at two call sites`.
- **Failure scenario:** A future engineer acts on OQ-39 — the feature's own recorded blind spot, that no test can tell Instant.now() from getCreatedAt() or Instant.EPOCH — and reads its Suggested path, which says the plan prices the fix at two call sites with no schema change. They change `markDone(boolean)` to `markDone(boolean, Instant)`. Call site 1, TaskService.java:155, is a one-line change. Call site 2 is Subtask.java:66, inside the constructor, which has no Instant to pass. Only three continuations exist and none matches the quoted price: (a) add a 5th `Instant` parameter to the constructor — directly forbidden by contracts/subtask-completion.md:58 ("no constructor may take completedAt"), and it touches 39 `new Subtask(...)` sites, 38 of them tests, which is precisely the 30-site cost the plan itself used at plan.md:78 to REJECT alternative 1's stronger variant; (b) leave the constructor calling `Instant.now()` internally — then `POST /tasks/{taskId}/subtasks` with `{"name":"x","done":true}` still stamps from the wall clock and stays exactly as unpinnable as today, so the spec's dedicated "A subtask created already done records the moment" scenario (added, per the spec's boundary table, because "without this one, the add path could ship recording nothing") remains uncovered and OQ-39 is only half closed; (c) have TaskService.addSubtask construct with done=false and then call markDone(true, now) — this works and respects the contract, but it is a THIRD writer of the flag, reintroducing exactly the multi-call-site defect the compile-time guard was bought to prevent, and it silently leaves all 38 test constructors passing `true` on the wall clock. The recorded cost is therefore wrong in every branch, and it is wrong in the one artifact a human will consult to decide whether to close the feature's largest acknowledged hole.
- **Fix:** Correct plan.md:117 and plan.md:104 to state the real price: the update path is two call sites, but pinning the ADD path additionally requires either a constructor signature change (39 `new Subtask(...)` sites, and a waiver of the contract's line 58) or a third markDone call site in TaskService.addSubtask. Then amend OQ-39's Suggested path in catalogs/open-questions.md:509 with the same correction, and reclassify the reversibility row from a bare 'reversible' to 'reversible for the update path; the add path costs a constructor change the contract currently forbids.'

### [minor] FR-20 is catalogued as `speccing` after spec, plan, tasks and implement are all done, and its changelog row is out of date order

- **Axis:** traceability
- **Kind:** traceability
- **Evidence:** catalogs/requirements.md:31 ends "… | US-4.1 | speccing |". It is the only non-`done` status in the table (19 `done`, 1 `speccing`). All seven tasks are ticked, `mvn -B verify` is green at 429 tests, and the pipeline is at feat-029.audit. Separately, the new History row (catalogs/requirements.md:59, "2026-09-03 | v4") is inserted between the 2026-08-26 v3 row and two 2026-08-30 v3 rows, breaking the table's date ordering.
- **Failure scenario:** A reader auditing 'which requirements are outstanding' from catalogs/requirements.md sees FR-20 as still being specced, and cannot tell from the catalog that the API half is code-complete on a branch — the exact state the catalog exists to make cheap to resume from (CLAUDE.md §6, artifacts over memory). The mis-ordered changelog row makes the same table unusable as a chronology.
- **Fix:** Set FR-20's status to reflect reality now (there is no `building` precedent in this table, so either introduce it explicitly or flip to `done` at the pair's delivery and say so in the row), and move the v4 History row after the two 2026-08-30 entries.

### [minor] Spec scenario 3's second clause — name and planned dates survive an un-completion — is asserted by no test

- **Axis:** traceability
- **Kind:** test-coverage
- **Evidence:** spec.md:119: "And the subtask is still present in the task, with its name and planned dates unchanged". The three tests that un-complete assert only the moment and the percentage: SubtaskTest.java:65 markDone_doneToNotDone_erasesTheMoment (assertNull on completedAt only); TaskServiceTest.java:409 updateSubtask_uncompleting_erasesTheMomentAndStatusFollows (assertNull + assertEquals(0, status)); TaskResourceTest.java:971 subtaskCompletion_unmarkingDone_clearsTheMomentOnTheWire (done=false, completedAt null). The two pre-existing un-mark tests (TaskServiceTest.java:158, TaskResourceTest.java:221) use dateless subtasks and assert only the percentage. Presence is covered (orElseThrow at TaskServiceTest.java:418); name and dates are not.
- **Failure scenario:** Add `this.startDate = null; this.endDate = null;` to the true→false branch of Subtask.markDone. All 429 tests stay green: every test that performs a true→false transition either uses a dateless subtask or asserts nothing about dates afterwards. In production, un-ticking a subtask silently wipes its planned dates, and because TaskService.updateSubtask calls markDone *after* setStartDate/setEndDate (TaskService.java:150-155) the client's echoed dates are overwritten too — then TaskDatesRecalculator collapses the parent task's derived span (BR-07). The spec anticipated exactly this and wrote the clause; nothing enforces it.
- **Fix:** Extend TaskServiceTest.updateSubtask_uncompleting_erasesTheMomentAndStatusFollows with assertions that the subtask still reports name "A", SEP_1/SEP_5, and that task.getStartDate()/getEndDate() are unchanged — one added block, no new test.

### [minor] tasks.md's coverage table cites a task that structurally cannot cover the clause it claims, and plan/tasks disagree on the domain-unit count

- **Axis:** traceability
- **Kind:** traceability
- **Evidence:** tasks.md:129 maps "Completing does not move the planned dates" to T-02 only. T-02's files are Subtask.java, TaskService.java and SubtaskTest.java; SubtaskTest constructs no Task, so it cannot assert spec.md's second clause "And the task's derived start and end dates are unchanged". That clause *is* in fact covered — by the pre-existing TaskServiceTest.java:326 updateSubtask_doneFlipOnly_datesUnchangedAndOnlyTheFlipFactFires (asserts SEP_1/SEP_5 on the task after a done-flip) — which no feature artifact names. Also plan.md:159 says "Seven of the twelve scenarios land here" for the domain unit seam while tasks.md T-02 and SubtaskTest's own javadoc both say six; the real number of spec scenarios reached by SubtaskTest is six (1,2,3,4,5,7). Full verified map, scenario → test method: (1) Completing records the moment → SubtaskTest.markDone_notDoneToDone_recordsTheMoment + TaskServiceTest.updateSubtask_completing_recordsTheMomentAndStatusStillCountsDone; (2) Created already done → SubtaskTest.constructor_bornDone_recordsTheMoment + TaskServiceTest.addSubtask_bornDone_recordsTheMoment; (3) Un-completing erases → SubtaskTest.markDone_doneToNotDone_erasesTheMoment + TaskServiceTest.updateSubtask_uncompleting_erasesTheMomentAndStatusFollows + TaskResourceTest.subtaskCompletion_unmarkingDone_clearsTheMomentOnTheWire (second clause uncovered — separate finding); (4) Completing again → SubtaskTest.markDone_completedAgainAfterUncompleting_recordsAMoment; (5) Done unchanged → SubtaskTest.markDone_alreadyDone_leavesTheMomentUntouched (assertSame) + TaskResourceTest.subtaskCompletion_readModelCarriesTheMomentAndThePlannedEnd (rename, read-vs-read); (6) Rescheduling keeps the moment → TaskServiceTest.updateSubtask_reschedulingACompletedSubtask_keepsItsMoment; (7) Completing does not move planned dates → SubtaskTest.markDone_completing_leavesThePlannedDatesAlone (subtask half) + TaskServiceTest.updateSubtask_doneFlipOnly_datesUnchangedAndOnlyTheFlipFactFires (task half, uncited); (8) Read model → TaskResourceTest.subtaskCompletion_readModelCarriesTheMomentAndThePlannedEnd; (9) Pre-FR-20 row → TaskRepositoryTest.legacyDoneSubtask_hasNoMoment_andIsNeverBackFilled; (10) Not client-writable → TaskResourceTest.subtaskCompletion_clientSuppliedMoment_rejected; (11) Portuguese rejection → TaskResourceTest.subtaskCompletion_rejection_resolvesInPortugueseLocale; (12) Cross-tenant → TaskResourceTest.foreignTenant_readUpdateDeleteAndSubtaskOps_behaveLikeMissing.
- **Failure scenario:** Someone deleting or rewriting TaskServiceTest.updateSubtask_doneFlipOnly_datesUnchangedAndOnlyTheFlipFactFires — a feat-010-era test with a name about events, not about FR-20 — consults tasks.md, sees scenario 7 attributed entirely to T-02/SubtaskTest, and concludes the deletion costs feat-029 nothing. It in fact removes the only assertion that a completion does not move the task's derived span, and no feat-029 test replaces it.
- **Fix:** Correct tasks.md:129 to name both seams for scenario 7 and cite updateSubtask_doneFlipOnly_datesUnchangedAndOnlyTheFlipFactFires by name; reconcile plan.md:159 to six.

### [minor] The no-backfill rule never travels TaskService.updateSubtask — the path a client actually uses

- **Axis:** traceability
- **Kind:** test-coverage
- **Evidence:** spec.md's pre-FR-20 scenario reads "When the task is **read** … And after an **update** that leaves it done, it still reports no completion moment". The sole test, TaskRepositoryTest.legacyDoneSubtask_hasNoMoment_andIsNeverBackFilled, reads through TaskRepository rather than SubtaskDto/the wire, and its 'update' is a direct domain call — TaskRepositoryTest.java:79 `before.markDone(true); before.setName("Renamed");` — not `service.updateSubtask(...)`. So the only assertion in the suite that a legacy row is never back-filled exercises the same guard clause SubtaskTest already covers, one layer below the layer the scenario names.
- **Failure scenario:** A later 'repair the legacy rows' change adds to TaskService.updateSubtask: `if (input.doneOrFalse() && subtask.isDone() && subtask.getCompletedAt() == null) subtask.recordCompletion(Instant.now());`. Every test stays green, because no test drives a legacy row through updateSubtask. Production then stamps a fabricated moment on every pre-FR-20 subtask on its next save — manufacturing exactly the lateness verdict the spec, the migration comment and plan.md's reversibility table call the feature's one irreversible decision.
- **Fix:** In the same test, replace the direct `before.markDone(true)` with a `service.updateSubtask(taskId, subtaskId, new SubtaskInput("Renamed", null, null, true, null, null))` call (or add a second test doing so), and assert the null moment on the wire via SubtaskDto rather than only on the entity.

### [minor] New import in TaskRepositoryTest breaks the configured import-order rule, and lint cannot see it

- **Axis:** Check 3
- **Kind:** code-standards-import-order
- **Evidence:** src/test/java/com/notebox/api/infrastructure/persistence/TaskRepositoryTest.java:12 — commit c1847f8 added `import java.util.UUID;` at line 12, directly after `import jakarta.inject.Inject;` (line 11), followed by a blank line at 13 and `import jakarta.persistence.EntityManager;` at 14. constitution/03-code-standards.md §Import order requires grouping by top-level package family with one blank line between groups and alphabetical order within a group; config/checkstyle.xml:40-46 encodes exactly that as `ImportOrder` with groups=java,javax,jakarta,com.notebox,* and separated=true, ordered=true.
- **Failure scenario:** Set pom.xml:130 `<includeTestSourceDirectory>` to true (or otherwise point Checkstyle 10.20.1 with config/checkstyle.xml at src/test/java) and the `validate` phase of `mvn -B verify` fails with violationSeverity=error: ImportOrder reports a wrong-order error on TaskRepositoryTest.java:12 because `java.util.UUID` (group index 0) follows `jakarta.inject.Inject` (group index 2), and an extra-separation error on line 14 because a blank line splits the jakarta group in two. Today verify is green only because pom.xml:130 hard-codes includeTestSourceDirectory=false, and that explicit POM value overrides the -Dcheckstyle.includeTestSourceDirectory user property — I confirmed this by running `mvn -B checkstyle:check -Dcheckstyle.includeTestSourceDirectory=true`, which reported BUILD SUCCESS while target/checkstyle-result.xml contained zero <file> entries, i.e. it scanned nothing. So the implementer's 'verify green' claim is true but carries no signal about test-source style: the rule the constitution names is structurally unable to fire on the file this feature added.
- **Fix:** Move `import java.util.UUID;` up beside `import java.util.List;` (line 9) and delete the blank line at line 13 so the jakarta family is one contiguous, alphabetical group: java.util.List, java.util.UUID / blank / jakarta.inject.Inject, jakarta.persistence.EntityManager.

### [minor] Stray double blank line left where setDone was replaced by markDone

- **Axis:** Check 3
- **Kind:** formatting-leftover
- **Evidence:** src/main/java/com/notebox/api/domain/Subtask.java:159-162 — markDone's closing brace is at 159, then two consecutive blank lines (160, 161), then `public void setCard(Card card)` at 162. Every other member boundary in the file uses a single blank line. The diff hunk shows the extra line was introduced when `setDone` was rewritten into `markDone`; constitution/03-code-standards.md §General requires the smallest diff with nothing left behind, and §Tooling delegates formatting to Spotless wired into verify.
- **Failure scenario:** This is cosmetic — it changes no behaviour, and I am labelling it minor rather than dressing it up. What makes it reportable is that nothing in the build can catch it: pom.xml declares only maven-checkstyle-plugin (lines 113-141), there is no Spotless plugin at all despite 03-code-standards.md §Tooling naming it as the formatter wired into verify, and config/checkstyle.xml declares no EmptyLineSeparator or RegexpMultiline module (its full module list is LineLength, TypeName, MethodName, MemberName, ParameterName, LocalVariableName, ConstantName, PackageName, ImportOrder, UnusedImports, RedundantImport, JavadocType, EqualsHashCode, StringLiteralEquality, EmptyBlock, MissingOverride). So `mvn -B verify` is green and stays green, and the artefact reaches the trunk. Add the Spotless plugin the constitution names and verify fails on Subtask.java with a formatting diff at line 160.
- **Fix:** Delete one of the two blank lines at Subtask.java:160-161. Separately worth an OQ or backlog item, not a fix inside this feature: 03-code-standards.md §Tooling promises Spotless in verify and the pom has never had it, so the 'formatter enforces it' half of the standard is currently fiction.

### [minor] plan.md's "No existing test is rewritten" is contradicted by its own approved successor, and the BR-06 freeze argument it supports is void

- **Axis:** Check 5
- **Kind:** plan-was-wrong / falsified blast radius
- **Evidence:** plan.md:142 — "**Tests.** No existing test is rewritten... Everything added is new." plan.md:135 builds on it: "the entire existing service suite becomes an unmodified regression guard on the frozen BR-06/BR-07 — an unchanged file cannot have acquired a new input to the percentage." But tasks.md — approved the same day, 2026-09-03 — instructs the opposite in two places: T-04 ("The key goes on `TaskMessageCoverageTest`'s hand-maintained list") and T-07 ("**Extends the existing foreign-tenant sweep**"). Reality: four existing test files modified — TaskResourceTest (+146/-1), TaskServiceTest (+86/-5), TaskMessageCoverageTest (+1/-0), TaskRepositoryTest (+48/-0) — and six existing method/helper bodies changed (TaskServiceTest:93, :97 helpers; :332, :512, :541 tests; TaskResourceTest:353). Only the implementer flagged it, in commit message 361885c, and only for one of the two files.
- **Failure scenario:** instructions/feature-audit.md check 5 tells the auditor to diff against the plan's blast radius. An auditor doing exactly that reads plan.md:142, sees "no existing test is rewritten", and treats every test-side hunk as new — accepting without inspection the five re-arity'd SubtaskInput sites in the file that carries the BR-06 evidence. Concretely, plan.md:135's proof that BR-06 is frozen is "TaskServiceTest is an unchanged file"; TaskServiceTest is not unchanged, so the stated proof does not exist. (I verified the five edits by hand and they are mechanical — the new component sits at position 5 before `card`, the three appended sites previously passed `null` for `card` there and now pass `null, null`, and the two CardInput sites had `null` inserted ahead of the CardInput; no assertion, expected value, or argument meaning changed. The conclusion survives; the plan's argument for it does not.)
- **Fix:** Correct plan.md:142 to name the four existing test files and the SubtaskInput arity consequence, and restate plan.md:135's BR-06 argument on what is actually true — the service test's assertions are byte-identical and only constructor arity moved — rather than on the file being unmodified.

### [minor] The one behavioural edit to an existing test cannot fail unless an assertion three lines above it already failed

- **Axis:** Check 5
- **Kind:** test-evidence
- **Evidence:** src/test/java/com/notebox/api/api/TaskResourceTest.java:353 `foreignTenant_readUpdateDeleteAndSubtaskOps_behaveLikeMissing` is the only existing wire test whose assertions changed (+3/-1 at :378-381), and tasks.md T-07 makes it the sole evidence for the spec scenario "A subtask of another tenant cannot be completed". The added lines assert `subtasks[0].done == false` and `subtasks[0].completedAt` is null on B's task after A's PUT. Three lines earlier the pre-existing assertion already requires that PUT to return 404. TaskService.updateSubtask (src/main/java/com/notebox/api/application/task/TaskService.java:147) opens with `Task task = get(taskId);` — the tenant-scoped lookup throws before any field is touched — so no reachable state exists in which A's PUT mutates B's subtask and still returns 404.
- **Failure scenario:** Regress the tenant guard (drop the tenant filter in `get`): A's PUT returns 200, the pre-existing `.then().statusCode(404)` at :372 fails, and the run stops before the new assertions execute. Regress in the other direction (make markDone stamp on a no-op transition): B's subtask was created `done=false` and never transitions, so the new assertions still pass. There is no regression this edit detects that develop did not already detect — T-07's cross-tenant half adds coverage of zero scenarios. (T-07's other half, the new `subtaskUpdate_unauthenticated_rejected` at :389, is genuinely new C-02 evidence and does earn its place.)
- **Fix:** Either give the scenario a test that can fail on its own — have B's subtask already completed, then assert A's rejected PUT left the *recorded moment* byte-identical, which a write-then-authorize ordering would break — or drop the claim that T-07 covers it and let the pre-existing 404 assertion own that scenario openly.

### [minor] An unrelated notebox-web fix record and six other features' pipeline state ride this branch

- **Axis:** Check 5
- **Kind:** scope-creep
- **Evidence:** `git diff --name-status develop...feature/subtask-completion` includes `fixes/2026-09-02-grid-actions/record.md` (+49, commit c0685c3, "record for the grid-actions batch shipped in notebox-web PR #35") — a different repository's fix, named by no feat-029 artifact. workflow.json (+384/-…) additionally carries state transitions for six unrelated features: feat-023.review in_progress→done, feat-024.review pending→done, feat-025.review pending→done, feat-026-locale-management blocked→skipped along with all seven of its substeps and a rewritten deferral rationale, feat-027.review pending→done, feat-028.review pending→done, plus the creation of feat-030's whole lifecycle. Five of those flipped substeps hold `gate: human_approval`, and three were stamped completed within two seconds of each other (03:04:21, 03:04:22, 03:04:22) in commit 107d47d, the T-01 commit.
- **Failure scenario:** This audit returns `fail` and the branch is reopened, or the PR is closed unmerged, or it is merged and later reverted. The grid-actions fix record and six unrelated pipeline decisions go with it — including "feat-026 is skipped, not delivered", a product deferral with a human_approval gate that nothing else on disk records. Symmetrically, approving feat-029 approves those six features' gate transitions under feat-029's review, which no reviewer of this feature has any reason to open. Either direction moves state nobody decided to move.
- **Fix:** Land `fixes/2026-09-02-grid-actions/record.md` and the feat-023…028 workflow.json transitions on develop directly, and keep this branch's workflow.json diff to the feat-029 and feat-030 subtrees. Separately, the batch of human_approval substeps closed in one second belongs to the gate axis — flag it there.

### [minor] NFR-06 and its coverage test were never considered, though the feature publishes a new schema field and precedent asserts exactly that

- **Axis:** Check 5
- **Kind:** plan-was-wrong / missing scope
- **Evidence:** SubtaskDto.java:20 adds a published wire component, `completedAt`. The project declares `api_docs: openapi` (workflow.json → project.comments) and NFR-06 (catalogs/requirements.md:46) requires the OpenAPI document in sync. src/test/java/com/notebox/api/api/OpenApiCoverageTest.java:73 sets the precedent for a schema-field change: `assertTrue(document.contains("itemCount") && document.contains("typesUsed") && document.contains("averageStatus"), "the feat-016 aggregate fields are published in the schema (OQ-27, NFR-06)")`. feat-029 changed the same kind of surface and asserted nothing: OpenApiCoverageTest is untouched, and grepping spec.md, plan.md, data-model.md, contracts/subtask-completion.md and tasks.md for "openapi"/"NFR-06" returns zero hits.
- **Failure scenario:** Add `@Schema(hidden = true)` to SubtaskDto.completedAt, or a Jackson mixin that hides it from schema generation while leaving serialization intact. Every feat-029 test still passes — TaskResourceTest asserts against `/api/tasks` responses, never against `/q/openapi` — so `mvn -B verify` is green while the document served at `/q/openapi` no longer publishes the field. feat-030's client author, whose only contract is that document, codes against a SubtaskDto with no completion moment.
- **Fix:** Add one line to `openApiDocumentsTaskEndpoints` asserting the document contains `completedAt`, in the shape OpenApiCoverageTest:73 already uses for feat-016, and record OpenApiCoverageTest in the blast radius. If the panel decides NFR-06 is satisfied by generation alone, say so in the plan rather than leaving the precedent silently unfollowed.

### [minor] catalogs/requirements.md still records FR-20 as `speccing` with implementation complete

- **Axis:** Check 5
- **Kind:** traceability
- **Evidence:** catalogs/requirements.md:31 — the FR-20 row ends `| US-4.1 | speccing |`. Every other requirement in the table (FR-01…FR-19) reads `done`; `speccing` appears exactly once, in the row this feature added and never advanced. workflow.json has feat-029.implement = done with all seven tasks done and feat-029.audit = in_progress.
- **Failure scenario:** Anyone consulting the catalog — the operational projection of the PRD, and the source instructions/feature-audit.md check 1 walks for traceability — reads that FR-20 is still being specified while its code sits on a branch awaiting review. If this audit passes and epics.md moves US-4.1 to `delivered` as the instructions direct, the two catalogs contradict each other permanently, and no `wf` command reconciles them. (FR-20 legitimately cannot read `done` until feat-030 ships the web half, which is precisely why it needs an accurate intermediate value rather than a stale one.)
- **Fix:** Advance the FR-20 status to the value this catalog uses for a requirement whose API half is built and whose web half is not — introduce and document one if none exists — and check it again at feat-030's audit.

### [minor] OQ-38/39/40 were added with no History rows and no entry in the severity index, so two open questions are invisible to anyone reading the catalog's own summary

- **Axis:** open-questions
- **Kind:** catalog-hygiene
- **Evidence:** catalogs/open-questions.md:363 — the History table's last row is `| 2026-09-02 | OQ-37 opened: ... |`; there is no row for OQ-38 (opened+resolved 2026-09-02), OQ-39 or OQ-40 (both opened 2026-09-03). Every OQ from OQ-11 through OQ-37 has one, including OQ-35/36/37 opened two days earlier. catalogs/open-questions.md:11-15 — the 'Overview by severity' index still reads `🔴 Blocker | — `, `🟡 Important | — `, `🟢 Tactical | OQ-26`, `✅ Resolved | OQ-01 … OQ-25`, listing neither OQ-39 nor OQ-40. Confirmed by git: the whole 73-line catalog addition landed in one commit, bfa66ab, and no implementation commit revisited it.
- **Failure scenario:** The `release` step (or any human) opens catalogs/open-questions.md to answer what is still outstanding. The severity index at the top reports zero open Blocker and zero open Important questions and one Tactical (OQ-26 — itself already ✅ resolved 2026-08-28), and the History table's most recent entry is OQ-37 on 2026-09-02. Both say the catalog has nothing new. In fact OQ-39 — 'no test can tell a correct completion moment from a wrong one', a known-untested correctness hole in code about to be published — and OQ-40 — unpinned JDBC time zone, which shifts lateness verdicts near midnight — are open and reachable only by scrolling to lines 492 and 517 of a 540-line file. The index and the History are the two surfaces a reader trusts to be complete, and for this feature's output both are silently empty. (The index staleness for OQ-27…OQ-37 and the mislabelled OQ-26 are PRE-EXISTING and not introduced here; the three missing History rows are this feature's.)
- **Fix:** Add three History rows (2026-09-02 OQ-38 opened+resolved; 2026-09-03 OQ-39 opened; 2026-09-03 OQ-40 opened) matching the format used for OQ-23…OQ-37, and list OQ-39 and OQ-40 in the Overview-by-severity index. While there, correct OQ-26's row in that index — it is listed as open-Tactical but resolved on 2026-08-28 — and note the pre-existing gap for OQ-27…OQ-37 as its own cleanup rather than folding it into this feature's diff.

### [suspicion] C-01's cross-tenant evidence never exercises the subtask ADD path, which this feature made newly stateful

- **Axis:** compliance
- **Kind:** test-coverage
- **Evidence:** src/test/java/com/notebox/api/api/TaskResourceTest.java:353 foreignTenant_readUpdateDeleteAndSubtaskOps_behaveLikeMissing issues, as tenant A against tenant B's task: GET /tasks/{id}, PUT /tasks/{id}, DELETE /tasks/{id} and PUT /tasks/{id}/subtasks/{subtaskId} — but never POST /tasks/{id}/subtasks. The spec (features/029-.../spec.md, C-01 pre-flight) names its evidence as 'the existing foreign-tenant sweep that already exercises subtask operations', and the spec's Scope explicitly puts the create-already-done path in scope ('whether it is created done or updated into it'). tasks.md T-07 claims C-01 coverage but only extends the sweep's B-side read assertion.
- **Failure scenario:** Cannot be made concrete, which is why this is a suspicion and not a finding. I traced the add path: src/main/java/com/notebox/api/application/task/TaskService.java:131 addSubtask calls get(taskId) (line 90), the same tenant-scoped repository choke point that updateSubtask uses, so a foreign-tenant POST /tasks/{taskB}/subtasks with {"done":true} already 404s and records no moment on B's task. The gap is in the evidence, not the behaviour: if a future refactor scoped updateSubtask but not addSubtask, tenant A could create a completed subtask inside tenant B's task and no test in the suite would fail.
- **Fix:** Add one request to the existing sweep in TaskResourceTest.foreignTenant_readUpdateDeleteAndSubtaskOps_behaveLikeMissing: given().header("Authorization", authA).body(subtaskJson("stolen", true)).post("/tasks/" + taskB + "/subtasks").then().statusCode(404), then assert B still reads hasSize(1) subtasks. Two lines, same test, no new fixture.

### [suspicion] BR-05's silent-side-effect clause is the one C-10 argument the spec did not answer

- **Axis:** compliance
- **Kind:** compliance-boundary
- **Evidence:** constitution/00-principles.md:66-68 BR-05 has two clauses: 'Deleting a record that holds user data (an annotation, task, type, group) is a deliberate, confirmed action; the system never deletes such data as a silent side effect of another operation.' The spec's C-10 verdict (features/029-.../spec.md, 'n/a, and the reasoning is not reversibility') answers only the first clause — it argues the OBJECT is a field, not a record. The spec's own Rollout hazard #1 then states the second clause's fact plainly: 'Any client that updates a subtask without echoing done silently destroys a recorded completion.' src/main/java/com/notebox/api/domain/Subtask.java markDone sets this.completedAt = null on any true→false transition, and src/main/java/com/notebox/api/application/task/TaskService.java:154 calls it with input.doneOrFalse(), so an omitted done erases the moment.
- **Failure scenario:** Cannot be made concrete against the constitution as written, which is why this is a suspicion. BR-05's object is enumerated ('an annotation, task, type, group') and completed_at is a server-written field on a surviving row, so C-10's trigger ('an irreversible delete or an administrative action') does not fire on the text. I confirmed the spec's supporting asymmetry claim is factually true: subtask DELETION is audited (TaskService.java:175, ACTION_SUBTASK_DELETED, asserted at TaskServiceTest.java:192), while this erasure writes no AuditLog row. The behavioural risk — a rename that omits done destroying the one unrecoverable fact on the subtask, with no audit row to reconstruct it from — is real, pre-declared, and gated behind the paired promotion with feat-030.
- **Fix:** No code change. If the human wants the second clause honoured, it is a C-10 change the spec already scoped for free: AuditLog carries actor, action, target and time and needs no new shape, so one auditLog.persistInTenant(new AuditLog(..., "SUBTASK_UNCOMPLETED", TARGET_SUBTASK, subtaskId)) in the wasDone && !isDone branch of TaskService.updateSubtask would close it. Otherwise, record the second-clause reading as an OQ so the argument is on file rather than unanswered.

### [suspicion] The message-coverage check that C-09 names as evidence cannot catch a blank value, the exact thing C-09 forbids

- **Axis:** compliance
- **Kind:** test-honesty
- **Evidence:** constitution/02-compliance.md C-09: 'no raw key or blank ever reaches a client'. The evidence the spec names is the message-coverage check. src/test/java/com/notebox/api/infrastructure/i18n/TaskMessageCoverageTest.java:41 asserts only assertTrue(en.containsKey(key), ...) and the same for pt — presence, never non-emptiness — while its own class javadoc claims each key 'must have its own value in each locale file'. src/main/java/com/notebox/api/api/error/ConstraintViolationMapper.java localize() catches MissingResourceException and returns the raw key, so a missing key degrades to a raw key on the wire and a blank value passes straight through.
- **Failure scenario:** Not concrete for this feature: I read both catalogs and the shipped values are non-blank and correct (messages.properties:94, messages_pt.properties:94), the key IS on the hand-maintained list (line 25 — the exact omission the spec pre-warned about was avoided), and TaskResourceTest.java:913 asserts the full Portuguese string on the wire. The weakness is pre-existing and covers all 18 keys, not something feat-029 introduced. Concrete shape if it ever bites: an editor writes 'task.subtask.completed_at.not_writable=' in messages.properties, the coverage test stays green, and an en-locale client gets a 400 whose violation message is the empty string.
- **Fix:** Strengthen the shared assertion once, outside this feature: assertFalse(en.getProperty(key).isBlank(), ...) alongside the containsKey check, in TaskMessageCoverageTest and its Annotation/Group siblings. Backlog item, not a blocker on feat-029.

